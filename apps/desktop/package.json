{
  "name": "openconvert-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The OpenConvert desktop shell. Svelte 5 over Vite, with no runtime dependency beyond @tauri-apps/api.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "tauri": "tauri",
    "preview:ui": "vite --config vite.preview.config.ts"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tauri-apps/cli": "^2.0.0",
    "@types/node": "^22.10.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
