node_modules/
dist/

# The shell is its own cargo workspace (see src-tauri/Cargo.toml), so it has its
# own target directory and its own lock file location.
src-tauri/target/

# Generated by tauri-build from tauri.conf.json + capabilities/.
src-tauri/gen/schemas/

# Browser preview of the UI (vite.preview.config.ts). Never shipped.
preview-dist/
